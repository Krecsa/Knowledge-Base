# Шпаргалка по каталогам Linux

| **Описание назначения каталога**                             | **Где лежит** | **Примеры**                              |
| ------------------------------------------------------------ | ------------- | ---------------------------------------- |
| Базовые исполняемые файлы (команды) для всех пользователей.  | **`/bin`**    | `ls`, `cp`, `bash`                       |
| Системные утилиты для администрирования (требуют `sudo`).    | **`/sbin`**   | `fdisk`, `iptables`, `init`              |
| Библиотеки для программ из `/bin` и `/sbin`.                 | **`/lib`**    | `libc.so` (библиотека языка C)           |
| Аналог `Program Files`).                                     | **`/usr`**    | `/usr/bin`, `/usr/lib`, `/usr/share`     |
| Конфигурационные файлы системы и программ.                   | **`/etc`**    | `/etc/passwd`, `/etc/network/interfaces` |
| Переменные данные (логи, кэш, почта).                        | **`/var`**    | `/var/log`, `/var/www` (веб-серверы)     |
| Временные файлы (очищаются после перезагрузки).              | **`/tmp`**    |                                          |
| Файлы загрузчика (ядро, initramfs, GRUB).                    | **`/boot`**   | `vmlinuz-5.15.0-76-generic`              |
| Файлы устройств (жёсткие диски, USB, виртуальные терминалы). | **`/dev`**    | `/dev/sda`, `/dev/tty1`                  |
| Виртуальная ФС с информацией о процессах и системе.          | **`/proc`**   | `/proc/cpuinfo`, `/proc/meminfo`         |
| Информация о ядре и устройствах.                             | **`/sys`**    |                                          |
| Домашние каталоги пользователей.                             | **`/home`**   | `/home/alice`, `/home/bob`               |
| Домашний каталог суперпользователя (не путать с `/`!).       | **`/root`**   |                                          |
| Временные точки монтирования (например флешек).              | **`/mnt`**    | **`/mnt/usb`**                           |
| Автомонтирование устройств (CD/DVD, USB).                    | **`/media`**  | `/media/usb-drive`                       |
| Дополнительное ПО (часто проприетарное)                      | **`/opt`**    | `/opt/google/chrome`                     |
## **Сравнение с Windows и macOS**

| Назначение                  | Linux              | Windows                           | macOS                               |
| --------------------------- | ------------------ | --------------------------------- | ----------------------------------- |
| Домашняя папка пользователя | `/home/user`       | `C:\Users\user`                   | `/Users/user`                       |
| Глобальные конфиги          | `/etc`             | `C:\ProgramData`, реестр          | `/etc` → `/private/etc`             |
| Системные команды           | `/bin`, `/usr/bin` | `C:\Windows\System32`             | `/bin`, `/usr/bin` (симлинки)       |
| Блочные устройства          | `/dev/sda1`        | `\\.\PhysicalDrive0`              | `/dev/disk0s1`                      |
| Съёмные носители            | `/mnt`, `/media`   | `D:\`, `E:\`                      | `/Volumes`                          |
| Временные файлы             | `/tmp`             | `C:\Windows\Temp`                 | `/tmp` → `/private/tmp`             |
| Логи                        | `/var/log`         | `C:\Windows\Logs`, `Event Viewer` | `/var/log` → `/private/var/log`     |
| Программное обеспечение     | `/opt`             | `C:\Program Files`                | `/Applications` (GUI), `/opt` (CLI) |
| Пользовательские настройки  | `~/.config`        | `%APPDATA%`                       | `~/Library/Preferences`             |
| Кэш пользователя            | `~/.cache`         | `%LOCALAPPDATA%\Temp`             | `~/Library/Caches`                  |
### **Основные команды Linux для работы с файлами и директориями**

| Описание                         | Команда | Пример использования               |
| -------------------------------- | ------- | ---------------------------------- |
| Показать текущую директорию      | `pwd`   | `pwd`                              |
| Список файлов и папок            | `ls`    | `ls` / `ls -la`                    |
| Перейти в папку                  | `cd`    | `cd /home/user`                    |
| Создать папку                    | `mkdir` | `mkdir NewFolder`                  |
| Создать файл                     | `touch` | `touch file.txt`                   |
| Копировать файл/папку            | `cp`    | `cp file.txt /backup/`             |
| Переместить/переименовать        | `mv`    | `mv old.txt new.txt`               |
| Удалить файл                     | `rm`    | `rm file.txt`                      |
| Удалить пустую папку             | `rmdir` | `rmdir EmptyFolder`                |
| Удалить папку рекурсивно         | `rm -r` | `rm -r FolderWithFiles`            |
| Просмотреть содержимое файла     | `cat`   | `cat file.txt`                     |
| Просмотр файла с прокруткой      | `less`  | `less large_file.log`              |
| Вывести первые 10 строк файла    | `head`  | `head file.log`                    |
| Вывести последние 10 строк файла | `tail`  | `tail -f log.txt` (режим слежения) |
| Поиск файлов                     | `find`  | `find / -name "*.txt"`             |
| Очистить экран терминала         | `clear` | `clear`                            |
## Категории пользователей и типы прав

**Категории пользователей**:

| Категория      | Кто это                            |
| -------------- | ---------------------------------- |
| **User (u)**   | Владелец файла                     |
| **Group (g)**  | Пользователи из группы владельца   |
| **Others (o)** | Все остальные пользователи системы |
Типы прав:

| Символ | Право   | Значение                                    | Число |
| ------ | ------- | ------------------------------------------- | ----- |
| `r`    | read    | Чтение файла или просмотр содержимого папки | 4     |
| `w`    | write   | Запись или изменение файла/папки            | 2     |
| `x`    | execute | Выполнение файла или вход в папку           | 1     |
Примеры:

`chmod 777 file` - Все могут читать, изменять и выполнять файл.
`chmod 755 file` - Владелец имеет все права, остальные — только чтение и выполнение.
`chmod 644 file` - Владелец может читать и изменять, остальные — только читать.
## **Следует запомнить**

**`ls`** - Выводит список файлов и папок в текущей директории

**`ls -a`** - Показывает все файлы и папки в текущей директории, включая скрытые
- **`.` (одна точка)** — это ссылка на **текущую директорию**
- **`..` (две точки)** — это ссылка на **родительскую директорию**

**`cd`** -  Перемещение между директориями

**`cat`** - Выводит последовательно указанные файлы, объединяя их в единый поток

**`cp`** - Копирование файла

`rm` - Удаление файла

**`touch`** - Создать файл в выбранной директории

**`nano`** - Текстовый редактор. **CTRL + O** для сохранения файла

**`sudo`** - Временно дает права root и записывает все действия в `/var/log/auth.log`

**`/var/log`** - Здесь можно найти логи

**`/etc/passwd`** - Здесь хранится основная информация о пользователях и логины

**Обновление:**
sudo apt update обновит пакеты  
sudo apt full-upgrade выполнит глубокое обновление  
sudo apt autoremove удалить ненужные пакеты и исправит несовместимости
sudo reboot перезагрузка

